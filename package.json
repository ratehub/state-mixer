{
  "name": "state-mixer",
  "version": "0.0.1",
  "description": "get an array of interesting states",
  "main": "index.js",
  "scripts": {
    "test": "tape test.js | tap-spec",
    "test:watch": "nodemon -w *.js -x 'tape test.js | tap-spec'"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.11.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.8.0"
  },
  "dependencies": {
    "combinations": "^0.1.1"
  }
}
